import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://onchch.github.io',
  // Change the line above to your custom domain once you have one,
  // e.g. 'https://riverwijaya.com'
});
