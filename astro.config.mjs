import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://onchch.github.io',
  // Change to your custom domain once set up, e.g.:
  // site: 'https://riverwijaya.com'
});
